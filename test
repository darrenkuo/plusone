ant -f build.xml

# download test.data if it doesn't exist

if [ ! -f /tmp/test.data ]; then
   cd /tmp/; wget http://inst.eecs.berkeley.edu/~darrenk/test.data
fi

cd -; java -cp Plusone.jar plusone.Main /tmp/test.data 0.9 0.2